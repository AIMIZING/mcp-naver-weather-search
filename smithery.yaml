startCommand:
  type: http
  http:
    port: 8000
    path: /mcp
  commandFunction: |-
    () => ({
      command: 'python',
      args: ['server_http.py'],
      env: {
        CACHE_TTL_SECONDS: '600',
        RATE_LIMIT_INTERVAL: '1.0',
        PORT: '8000'
      }
    })
